{% extends 'base.html.twig' %}

{% block title %}Mise à jour mot de passe{% endblock %}

{% block body %}

<div class="container mt-5">
    <h3 class="text-center">Modification du mot de passe</h3>
    <div class="col-lg-4 offset-lg-4 text-center  mt-4 preview">
        {{ form_start(form) }}

        {{ form_row(form.oldPassword) }}

        {{ form_row(form.newPassword) }}

        <button type="submit" class="btn btn-success mr-5 mt-2">Modifier le mot de passe</button>
        <a class="btn btn-primary ml-2 mt-2" href="{{ path('user_profile') }}" role="button">Retour</a>

        {{ form_end(form) }}
    </div>

    {% endblock %}

    {% block javascripts %}
    {{  encore_entry_script_tags('app') }}
{% endblock %}